node_modules/
dist/
.staging/
.sherpa-build/state.json.tmp.*
*.log
tests/visual/diffs/
.playwright-cache/
playwright-report/
test-results/
dist-electron/
out/
# build/ ships installer build resources (icons, installer-scripts) that MUST
# be tracked. The legacy ignore was intended for a now-defunct ad-hoc /build
# bundler output. Per T-L7-01: keep build/icons + build/installer-scripts
# under version control; ignore only the transient subdirs that previously
# landed there (none currently).
coverage/
.DS_Store
.env
.env.local
.env.*.local
.vite/

# Bundled embedding model (large binary — regenerate via npm run model:download)
resources/models/**/*.onnx
resources/models/**/*.json
!resources/models/**/.gitkeep
