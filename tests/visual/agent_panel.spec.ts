import { test } from '@playwright/test';

// Disabled until Phase 4 creates the Agent Panel screen renderer.
test.describe.skip('Agent panel screen visual regression', () => {
  test('matches baseline within threshold', async () => {
    // Pending Phase 4 component implementation.
  });
});
