// F9 test workspace plugin — minimal entry point.
// Used by AC-T-L5-09-1/2/3 (discovery + UI row + trust modal).
'use strict';

export function initialize(_ctx) {
  // no-op for test fixture
}

export function capabilities() {
  return ['hook'];
}
